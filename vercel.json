{
  "framework": "nextjs",
  "rootDirectory": "apps/readest-app",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "echo 'NEXT_PUBLIC_APP_PLATFORM=web' > apps/readest-app/.env.web && touch apps/readest-app/.env.local && cd apps/readest-app && npx next build",
  "outputDirectory": "apps/readest-app/.next",
  "build": {
    "env": {
      "NEXT_PUBLIC_APP_PLATFORM": "web",
      "PNPM_VERSION": "10.33.0"
    }
  }
}
